import type {
  PaperRecord,
  ProjectRecord,
  TimelineEventRecord,
  TopicRecord,
  UpdateRecord,
} from './content/schema';

export type { PaperRecord, ProjectRecord, TimelineEventRecord, TopicRecord, UpdateRecord };
